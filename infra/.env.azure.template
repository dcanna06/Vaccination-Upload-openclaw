# AIR Bulk Vaccination Upload — Azure Environment Variables
#
# These are configured as App Service settings via setup-azure.sh.
# Key Vault references (@Microsoft.KeyVault...) are resolved automatically
# by Azure App Service when the app's managed identity has Key Vault access.
#
# This file is a reference only — do NOT deploy it as an .env file.

# ── Application ───────────────────────────────────────────────────────────────
APP_ENV=vendor
APP_SECRET_KEY=@Microsoft.KeyVault(SecretUri=https://air-vaccination-kv.vault.azure.net/secrets/app-secret-key)
FRONTEND_URL=https://air-vaccination-frontend.azurewebsites.net
LOG_LEVEL=INFO
LOG_FORMAT=json

# ── Database ──────────────────────────────────────────────────────────────────
DATABASE_URL=@Microsoft.KeyVault(SecretUri=https://air-vaccination-kv.vault.azure.net/secrets/database-url)
REDIS_URL=@Microsoft.KeyVault(SecretUri=https://air-vaccination-kv.vault.azure.net/secrets/redis-url)

# ── PRODA B2B Authentication ─────────────────────────────────────────────────
PRODA_JKS_BASE64=@Microsoft.KeyVault(SecretUri=https://air-vaccination-kv.vault.azure.net/secrets/proda-jks-base64)
PRODA_JKS_PASSWORD=@Microsoft.KeyVault(SecretUri=https://air-vaccination-kv.vault.azure.net/secrets/proda-jks-password)
PRODA_ORG_ID=@Microsoft.KeyVault(SecretUri=https://air-vaccination-kv.vault.azure.net/secrets/proda-org-id)
PRODA_DEVICE_NAME=@Microsoft.KeyVault(SecretUri=https://air-vaccination-kv.vault.azure.net/secrets/proda-device-name)
PRODA_CLIENT_ID=@Microsoft.KeyVault(SecretUri=https://air-vaccination-kv.vault.azure.net/secrets/proda-client-id)

# ── AIR API ───────────────────────────────────────────────────────────────────
AIR_CLIENT_ID=@Microsoft.KeyVault(SecretUri=https://air-vaccination-kv.vault.azure.net/secrets/air-client-id)

# ── Azure App Service (set automatically) ─────────────────────────────────────
WEBSITES_PORT=8000

# ── Frontend App Service ──────────────────────────────────────────────────────
# NEXT_PUBLIC_API_URL=https://air-vaccination-backend.azurewebsites.net
# WEBSITES_PORT=3000
